<!DOCTYPE html>
<html>

<head>
  <title>Taco Recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Taco Town 🌮</h1>

  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="chicken" name="choice">🍗</button>
    <button type="submit" value="beef" name="choice">🥩</button>
    <button type="submit" value="fish" name="choice">🐟</button>
  </form>
  <div id="recipeContainer" class="hidden">

    <!-- If no recipe yet, show: -->
     <% if(!locals.data){ %>
    <h2>Pick your favourite taco ingredient 👆</h2>
    <% }else{ %>
      <h2>Here's your taco recipe: </h2>
      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li>Protein: <%= data.ingredients.protein.name %>, preparation<%= data.ingredients.protein.preparation %></li>
        <li>Salsa: <%= data.ingredients.salsa.name%>, spiciness: <%= data.ingredients.spiciness%></li>
        <li>Topping:</li>
        <% for(var i=0;i<data.ingredients.toppings.length;i++){ %>
        <li> <%= data.ingredients.toppings[i].name %>, <%= data.ingredients.toppings[i].quantity %>, <%= data.ingredients.toppings[i].ingredients %> </li>
        <% } %>
      </ul>
    <% } %>

  </div>
</body>

</html>